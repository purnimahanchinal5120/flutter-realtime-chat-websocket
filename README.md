# flutter-realtime-chat-websocket
Real-time chat app built with Flutter and WebSocket using MVVM architecture. Features include live messaging, connection status handling, auto-reconnect, message delivery states (sending, sent, failed), and retry mechanism. Designed with clean UI and scalable state management.
